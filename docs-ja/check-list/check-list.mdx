---
id: check-list
title: Check List
description: Smart-Agent で人・車両・企業チェックの組み合わせを保存し、1件実行するか SmartBatch に送る
slug: /check-list
---

import { Globe, Users, Play, Layers } from 'lucide-react';

# Check List

複数の国で同じ SmartCheck サービスを何度も選び直しているなら、**その組み合わせを Check List として保存**してください。リストからチェックを試すか、SmartBatch に送れます。

Smart-Agent の **Check List** は [ai.verifik.co/check-list](https://ai.verifik.co/check-list) です。リストの保存は無料です。チェックを実行すると、他の API と同じようにクレジットが消費されます。

<img src="/img/check-list/check-list-flow.png" alt="Check List の流れ: 国を選ぶ、人・車両・企業を追加、チェックを試す、または SmartBatch を作成" />

リストの保存は無料です。チェックを実行すると、他の API と同じようにクレジットが消費されます。

## できること

<div className="row">
  <div className="col col--6">
    <div className="card">
      <div className="card__header">
        <h3><Globe size={20} style={{display: 'inline', marginRight: '8px'}} />国を選ぶ</h3>
      </div>
      <div className="card__body">
        <p>作成時に事業国を選びます。カタログはその国のサービスを表示します。</p>
      </div>
    </div>
  </div>
  <div className="col col--6">
    <div className="card">
      <div className="card__header">
        <h3><Users size={20} style={{display: 'inline', marginRight: '8px'}} />人・車両・企業</h3>
      </div>
      <div className="card__body">
        <p>身分証明、プレート、企業登記などを追加し、ドメインでカタログを絞り込みます。</p>
      </div>
    </div>
  </div>
</div>

<div className="row">
  <div className="col col--6">
    <div className="card">
      <div className="card__header">
        <h3><Play size={20} style={{display: 'inline', marginRight: '8px'}} />チェックを試す</h3>
      </div>
      <div className="card__body">
        <p>サービスを開き、項目を入れて実行します。クレジットはその呼び出しにかかり、リスト保存にはかかりません。</p>
      </div>
    </div>
  </div>
  <div className="col col--6">
    <div className="card">
      <div className="card__header">
        <h3><Layers size={20} style={{display: 'inline', marginRight: '8px'}} />SmartBatch を作成</h3>
      </div>
      <div className="card__body">
        <p>保存したサービスをファイルまたはキューとして SmartBatch に渡します。国が複数なら先に 1 つ選びます。</p>
      </div>
    </div>
  </div>
</div>

---

## Smart-Agent で

Verifik のクライアントアカウントでサインインします。Check List は SmartEnroll プロジェクトではなく、そのアカウントにリストを保存します。

### 1. あなたのリスト

ダッシュボードにリスト数、対象国、保存済みサービス数が表示されます。カードを開いて編集するか、新規作成します。

<img src="/img/check-list/dashboard.png" alt="統計と保存済みリストがある Check List ダッシュボード" />

リストがまだない場合は、国から始めます。

:::tip
作成後はこのダッシュボードに戻ります。新しいカードを開いてサービスを追加してください。
:::

---

### 2. チェックリストを作成

国を 1 つ以上選び、名前を付けます（例: **KYC Colombia**、**Fleet Chile**）。

<img src="/img/check-list/create.png" alt="国と名前でチェックリストを作成" />

国は作成時に決まります。ワークスペースでは後から変えません。

---

### 3. サービスを追加

リストを開きます。保存済みサービスは上です。下でカタログを検索し、**人** / **車両** / **企業** で絞り込みます。国際的な背景調査は一番下です。

<img src="/img/check-list/workspace.png" alt="保存済みサービスとカタログがある Check List ワークスペース" />

**選択**で追加し、**開く**で試します。下書き / 有効は整理用のラベルで、どちらも同じように動きます。

---

### 4. チェックを試す

サービスを開くと **概要**、**リクエスト**、**結果** があります。必須項目を入れて **実行** をクリックします。

<img src="/img/check-list/run-check.png" alt="チェック実行ドロワー" />

結果はそのカタログ API と同じ応答です。呼び出し後にクレジット残高が更新されます。

---

### 5. SmartBatch に送る

同じサービスをファイルやキューで回すときは **Create SmartBatch** をクリックします。SmartBatch は **国 1 つ** です。リストに複数ある場合は、開始する国を選びます。

<img src="/img/check-list/smartbatch-handoff.png" alt="SmartBatch 作成前に国を選ぶ" />

SmartBatch がその国、リスト名、選択したサービスで開きます。[SmartBatch](/verifik-ja/smart-batch) ウィザードで続けてください。

---

## Check List ではないもの

| 誤解 | 実際 |
| --- | --- |
| バッチ実行エンジン | Check List は組み合わせを**保存**します。[SmartBatch](/verifik-ja/smart-batch) がファイルやキューを実行します。 |
| ホスト型 KYC | SmartEnroll は別の enrollment 製品です。 |
| 試すたびに新規登録 | 実行はそのサービスの既存カタログ API を呼び出します。 |

---

## 関連

- [Check List API ガイド](/verifik-ja/check-list/api-companion) — バックエンドでリストを保存・取得
- [SmartBatch](/verifik-ja/smart-batch) — 保存した組み合わせをファイルまたはキューで実行
