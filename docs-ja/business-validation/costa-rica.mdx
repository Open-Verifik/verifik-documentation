---
id: costa-rica
title: コスタリカ法人（NITE）
description: GET /v2/cr/company で NITE による法人照会（KYB）。
slug: /business-validation/costa-rica
---

export const structuredData = {
	"@context": "https://schema.org",
	"@type": "TechArticle",
	headline: "コスタリカ法人（NITE）照会 | Verifik KYB API",
	description: "法人の NITE 照会: GET /v2/cr/company。",
	articleSection: "API ドキュメント",
	keywords: "コスタリカ NITE, KYB, v2/cr/company",
	about: {
		"@type": "Thing",
		name: "法人確認 API",
	},
};

<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(structuredData) }} />

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# コスタリカ法人照会（NITE）

### エンドポイント

```
https://api.verifik.co/v2/cr/company
```

**`documentType`**: `NITE`、**`documentNumber`**: NITE 番号。

### ヘッダー

| 名前 | 値 |
| --- | --- |
| Accept | `application/json` |
| Authorization | `Bearer <token>` |

### パラメータ

| 名前 | 型 | 必須 | 説明 |
| --- | --- | --- | --- |
| `documentType` | string | はい | `NITE` |
| `documentNumber` | string | はい | NITE 識別子 |

### リクエスト

<Tabs>
  <TabItem value="node" label="Node.js">

```javascript
import axios from "axios";

const { data } = await axios.get("https://api.verifik.co/v2/cr/company", {
  params: { documentType: "NITE", documentNumber: "3101122876" },
  headers: { Accept: "application/json", Authorization: `Bearer ${process.env.VERIFIK_TOKEN}` },
});
console.log(data);
```

  </TabItem>
</Tabs>

### レスポンス

<Tabs>
  <TabItem value="200" label="200">

```json
{ "data": {}, "signature": {}, "id": "" }
```

  </TabItem>
</Tabs>
