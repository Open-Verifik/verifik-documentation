---
id: chile-taxpayer
title: チリ — 納税者（RUT）
description: "RUT 納税者レコードの照会：税務向けの氏名フィールドで KYB・請求書照合に。"
slug: /identity/chile-taxpayer
---

export const structuredData = {
	"@context": "https://schema.org",
	"@type": "TechArticle",
	headline: "チリ納税者 RUT 照会 | Verifik API",
	description: "GET /v2/cl/taxpayer、documentType RUT。",
	articleSection: "API ドキュメント",
	keywords: "チリ RUT, v2/cl/taxpayer",
	about: {
		"@type": "Thing",
		name: "納税者照会 API",
	},
};

<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(structuredData) }} />

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# チリ納税者（RUT）照会

チリの **RUT** について、**納税者（税務）**向けの本人情報が必要なときに使います（民籍の **cédula** フローとは別）。**[チリにおける本人確認](/identity/chile)**（`v2/cl/cedula`）は **RUN／民籍** 照合向けで、本エンドポイントと併用します。

### Endpoint

```
https://api.verifik.co/v2/cl/taxpayer
```

**税務登録**に沿った氏名・識別子を返します。**KYB**、仕入先オンボーディング、**請求書**で RUT が **登録納税者**と一致する必要がある場面に適します。

### Headers

| Name | Value |
| --- | --- |
| Accept | `application/json` |
| Authorization | `Bearer <token>` |

### Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `documentType` | string | Yes | `RUT` |
| `documentNumber` | string | Yes | 税務識別子（サービスが正規化） |

### Request

<Tabs>
  <TabItem value="node" label="Node.js">

```javascript
import axios from "axios";

const { data } = await axios.get("https://api.verifik.co/v2/cl/taxpayer", {
  params: { documentType: "RUT", documentNumber: "263007905" },
  headers: { Accept: "application/json", Authorization: `Bearer ${process.env.VERIFIK_TOKEN}` },
});
console.log(data);
```

  </TabItem>
  <TabItem value="python" label="Python">

```python
import os, requests

url = "https://api.verifik.co/v2/cl/taxpayer"
headers = {"Accept": "application/json", "Authorization": f"Bearer {os.getenv('VERIFIK_TOKEN')}"}
params = {"documentType": "RUT", "documentNumber": "263007905"}
r = requests.get(url, headers=headers, params=params)
print(r.json())
```

  </TabItem>
</Tabs>

### Response

<Tabs>
  <TabItem value="200" label="200">

```json
{
  "data": {
    "arrayName": ["RICHARD", "DANIEL", "BARBERA", "MARICHAL"],
    "documentNumber": "263007905",
    "documentType": "RUT",
    "firstName": "RICHARD DANIEL",
    "fullName": "RICHARD DANIEL BARBERA MARICHAL",
    "lastName": "BARBERA MARICHAL"
  },
  "signature": {
    "dateTime": "April 9, 2026 3:02 PM",
    "message": "Certified by Verifik.co"
  },
  "id": "S40CG"
}
```

  </TabItem>
  <TabItem value="404" label="404">

```json
{
  "code": "NotFound",
  "message": "Record not found."
}
```

  </TabItem>
  <TabItem value="409" label="409">

```json
{
  "code": "MissingParameter",
  "message": "documentNumber maximum length exceeded.\n"
}
```

  </TabItem>
</Tabs>

### Notes

- 民籍の **RUN** 一致には **`v2/cl/cedula`**、真実源が **納税者 RUT** のときは **`v2/cl/taxpayer`** を使ってください。
