---
id: colombia-situacion-militar
title: 🇨🇴 コロンビア — 兵役状況
description: コロンビア市民証（CC）で兵役（situación militar）を照会します。
sidebar_label: 🇨🇴 兵役状況
slug: /identity/colombia-situacion-militar
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

### エンドポイント

```
GET https://api.verifik.co/v2/co/situacion-militar
```

**クエリ**で **`documentType=CC`** と **`documentNumber`** を送ります。**CC**（市民証）のみ有効です。

### ヘッダー(Headers)

| Name | Value |
| --- | --- |
| Accept | `application/json` |
| Authorization | `Bearer <token>` |

### パラメータ(Parameters)

| name | type | required | description |
| --- | --- | --- | --- |
| `documentType` | string | yes | 必ず `CC`。CE・PPT などはこのエンドポイントでは拒否されます。 |
| `documentNumber` | string | yes | 照会する市民証番号。 |

### リクエスト(Request)

<Tabs>
  <TabItem value="node" label="Node.js">

```javascript
import axios from "axios";

const { data } = await axios.get("https://api.verifik.co/v2/co/situacion-militar", {
	params: { documentType: "CC", documentNumber: "1121329661" },
	headers: {
		Accept: "application/json",
		Authorization: `Bearer ${process.env.VERIFIK_TOKEN}`,
	},
});
console.log(data);
```

  </TabItem>
  <TabItem value="python" label="Python">

```python
import os, requests

url = "https://api.verifik.co/v2/co/situacion-militar"
headers = {"Accept": "application/json", "Authorization": f"Bearer {os.getenv('VERIFIK_TOKEN')}"}
params = {"documentType": "CC", "documentNumber": "1121329661"}
r = requests.get(url, headers=headers, params=params)
print(r.json())
```

  </TabItem>
</Tabs>

### レスポンス(Response)

<Tabs>
  <TabItem value="200" label="200">

```json
{
	"data": {
		"adress": "ソースに応じた住所または案内テキスト",
		"documentNumber": "1121329661",
		"documentType": "CC",
		"fullName": "姓 名",
		"place": "記録上の場所テキスト",
		"remissSince": "該当する場合の日付または状態テキスト",
		"state": "兵役状況のテキスト"
	},
	"signature": {
		"message": "Certified by Verifik.co",
		"dateTime": "April 20, 2026 10:00 AM"
	},
	"id": "ABC12"
}
```

  </TabItem>
  <TabItem value="404" label="404">

```json
{
	"code": "NotFound",
	"message": "Record not found."
}
```

  </TabItem>
  <TabItem value="409" label="409">

```json
{
	"code": "MissingParameter",
	"message": "検証エラー（クエリ不足または documentType が CC 以外）。"
}
```

  </TabItem>
</Tabs>

`adress` は API が返す綴りのままです。

### 備考(Notes)

- **`documentType`** は **`CC`** のみ有効です。
- フィールド内容は公式ソース上のレコードにより異なる場合があります。
