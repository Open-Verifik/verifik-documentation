---
id: ontario-driver-license
title: 🇨🇦 オンタリオ — 運転免許
description: "オンタリオ（カナダ）の運転免許を文書番号で検証します。"
slug: /driver-validation/ontario-driver-license
---

export const structuredData = {
	"@context": "https://schema.org",
	"@type": "TechArticle",
	headline: "オンタリオ — 運転免許検証 | Verifik KYC API",
	description:
		"Verifik の KYC API を使用して、オンタリオ州（カナダ）の運転免許を検証します。公式ソースに直接接続し、運転免許データを数秒で確認できます。",
	articleSection: "API ドキュメント",
	keywords: "本人確認, KYC API, オンタリオ 運転免許 検証, カナダ, 公式データソース, 不正防止",
	about: {
		"@type": "Thing",
		name: "本人確認 API",
	},
};

<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(structuredData) }} />

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# 🇨🇦 オンタリオ — 運転免許

Verifik の運転免許検証 API を使用すると、文書番号により、オンタリオ州（カナダ）で発行された運転免許の真正性および有効性を検証できます。KYC（Know Your Customer）プロセスの効率化、不正防止、各種規制要件への対応を簡単に実現できるよう設計されています。

ドライバーの運転資格を、迅速・安全・自動的に確認したいビジネス向けに構築しました。

**この情報に安全にアクセスするには Authorization ヘッダーが必要です。**

## この API は何を検証しますか？

本 API はカナダ・オンタリオ州の公式記録と直接接続し、以下を検証します。

-   **運転免許番号**: オンタリオ州運転免許番号の真正性を検証します。
-   **免許ステータス**: 運転免許が現在有効かどうかを確認します。
-   **検証番号**: 該当の照会に紐づく検証番号を返します。

これらを検証することで、運転免許が真正かつ有効であることを確認でき、なりすましや不正のリスクを大幅に低減できます。

### エンドポイント

```
GET https://api.verifik.co/v2/ca/ontario/driver-license
```

オンタリオ運転免許サービスでは、カナダ・オンタリオ州で発行された運転免許に関する重要な情報を迅速に取得できます。文書番号を指定することで、免許ステータスや関連する検証番号などの詳細にアクセスできます。

身分証明や規制目的のためにオンタリオ州の運転免許の真正性を確認する必要がある場合に、本サービスは正確かつ最新の免許情報を効率的に提供します。

### ヘッダー（Headers）

| 名前          | 値                 |
| ------------- | ------------------ |
| Content-Type  | `application/json` |
| Authorization | `Bearer <token>`   |

### パラメーター（Parameters）

| 名前 | 型 | 必須 | 説明 | 例 |
| ---- | ---- | ---- | ---- | ---- |
| `documentNumber` | string | **はい** | 照会対象の運転免許番号。ドキュメント上の表記どおりに入力してください。 | `S123456789123456` |

### リクエスト（Request）

<Tabs>
  <TabItem value="javascript" label="JavaScript">

```javascript
import axios from 'axios';

const options = {
  method: 'GET',
  url: 'https://api.verifik.co/v2/ca/ontario/driver-license',
  params: {
    documentNumber: 'S123456789123456'
  },
  headers: {
    'Accept': 'application/json',
    'Authorization': 'Bearer <あなたのトークン>'
  }
};

try {
  const { data } = await axios.request(options);
  console.log(data);
} catch (error) {
  console.error(error);
}
```

  </TabItem>
  <TabItem value="python" label="Python">

```python
import requests

url = "https://api.verifik.co/v2/ca/ontario/driver-license?documentNumber=S123456789123456"

payload = {}
headers = {}

response = requests.request("GET", url, headers=headers, data=payload)

print(response.text)
```

  </TabItem>
  <TabItem value="php" label="PHP">

```php
<?php
require_once 'HTTP/Request2.php';
$request = new HTTP_Request2();
$request->setUrl('https://api.verifik.co/v2/ca/ontario/driver-license?documentNumber=S123456789123456');
$request->setMethod(HTTP_Request2::METHOD_GET);
$request->setConfig(array(
  'follow_redirects' => TRUE
));
try {
  $response = $request->send();
  if ($response->getStatus() == 200) {
    echo $response->getBody();
  }
  else {
    echo 'Unexpected HTTP status: ' . $response->getStatus() . ' ' .
    $response->getReasonPhrase();
  }
}
catch(HTTP_Request2_Exception $e) {
  echo 'Error: ' . $e->getMessage();
}
?>
```

  </TabItem>
  <TabItem value="swift" label="Swift">

```swift
var request = URLRequest(url: URL(string: "https://api.verifik.co/v2/ca/ontario/driver-license?documentNumber=S123456789123456")!,timeoutInterval: Double.infinity)
request.httpMethod = "GET"

let task = URLSession.shared.dataTask(with: request) { data, response, error in 
  guard let data = data else {
    print(String(describing: error))
    return
  }
  print(String(data: data, encoding: .utf8)!)
}

task.resume()
```

  </TabItem>
</Tabs>

### レスポンス（Response）

<Tabs>
  <TabItem value="200" label="200">

```json
{
  "data": {
    "documentNumber": "S123456789123456",
    "licenceStatus": "Valid",
    "verificationNumber": "123V5"
  },
  "signature": {
    "dateTime": "March 12, 2024 3:47 PM",
    "message": "Certified by Verifik.co"
  },
  "id": "8X9FD"
}
```

  </TabItem>
  <TabItem value="404" label="404">

```json
{
    "code": "NotFound",
    "message": "Record not found."
}
```

  </TabItem>
  <TabItem value="409" label="409 - パラメーター不足">

```json
{
  "code": "MissingParameter",
  "message": "missing documentNumber\n"
}
```

  </TabItem>
</Tabs>

---

## 一般的なユースケース

-   **フィンテックと銀行**: 口座開設や融資申請の際に運転免許を即時検証します。
-   **ライドシェアと配送**: プラットフォーム稼働前にドライバーを認証します。
-   **HR と採用**: 採用ワークフローの一部としてドライバー資格を検証します。
-   **保険と自動車**: 保険契約や請求処理の前に運転免許の有効性を確認します。
-   **フリート管理**: 運用開始前に法人ドライバーの運転資格を検証します。

## 公式ソースと信頼性

カナダ・オンタリオ州の公式運転免許システムと直接接続し、検証済みかつ最新の情報を提供します。
すべての照会は厳格なセキュリティおよび規制基準に基づいて処理されます。

## 主な利点

-   **自動化されたコンプライアンス**: ユーザー体験を損なうことなく検証チェックを効率化し、不正を防止します。
-   **即時の結果**: 数秒で照会を処理できるため、リアルタイムでのデジタルオンボーディングに最適です。
-   **信頼できるデータ**: 公式の州レコードから直接取得したデータを利用できます。
-   **容易な統合**: REST API または互換 SDK を通じて簡単に接続できます。

## コンプライアンスとセキュリティ

データの安全性を最優先します。Verifik は高度な暗号化（HTTPS / TLS 1.3）と厳格なプライバシー管理基準を採用し、機密性を確保します。
当サービスは可用性を 24 時間 365 日監視しており、チームのアクセスを安全に保つロールベースのアクセス制御を提供します。

## Verifik について

Verifik は、ラテンアメリカおよび北米における本人確認、コンプライアンス、不正防止のためのリーディングプラットフォームです。
当社の API は KYC、KYB、AML、生体検証プロセスを自動化し、コロンビア、メキシコ、ペルー、チリ、ウルグアイ、アルゼンチン、ブラジル、カナダなどの公式データソースとビジネスを接続します。
