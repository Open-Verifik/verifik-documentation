---
id: united-states-passport-entries
title: アメリカ — パスポート・米国入出国履歴
description: GET /v2/passport/us/entries。氏名・生年月日・パスポート番号などで照会します。
slug: /background-check/united-states-passport-entries
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

# アメリカ — パスポートと米国入出国（履歴）

**パスポートに紐づく米国への入出国履歴**（スクリーニング・コンプライアンス向け）をリクエストします。旅行者の本人確認フィールドとパスポート識別子をクエリパラメータで渡します。

## API リファレンス

### エンドポイント

```
GET https://api.verifik.co/v2/passport/us/entries
```

`firstName`、`lastName`、`dateOfBirth`、`passportNumber`、`passportCountry` をクエリに指定します。日付形式は契約どおり（例: `YYYY-MM-DD` または `dd/mm/yyyy`）。

### ヘッダー

| 名前          | 値                 |
| ------------- | ------------------ |
| Content-Type  | `application/json` |
| Authorization | `Bearer <token>`   |

### パラメーター

| 名前 | 型 | 必須 | 説明 |
| ---- | ---- | ---- | ---- |
| `firstName` | string | **はい** | パスポート表記の名。 |
| `lastName` | string | **はい** | パスポート表記の姓。 |
| `dateOfBirth` | string | **はい** | 生年月日（プロジェクトで合意した形式）。 |
| `passportNumber` | string | **はい** | パスポート番号。 |
| `passportCountry` | string | **はい** | 発行国（ISO コードまたは国名、統合仕様に従う）。 |

### リクエスト

<Tabs>
<TabItem value="javascript" label="JavaScript">

```javascript
import axios from "axios";

const options = {
  method: "GET",
  url: "https://api.verifik.co/v2/passport/us/entries",
  params: {
    firstName: "JOHN",
    lastName: "DOE",
    dateOfBirth: "1990-01-15",
    passportNumber: "P12345678",
    passportCountry: "CO",
  },
  headers: {
    Accept: "application/json",
    Authorization: "Bearer <your_token>",
  },
};

try {
  const { data } = await axios.request(options);
  console.log(data);
} catch (error) {
  console.error(error);
}
```

</TabItem>
<TabItem value="python" label="Python">

```python
import http.client
import urllib.parse

params = urllib.parse.urlencode({
    "firstName": "JOHN",
    "lastName": "DOE",
    "dateOfBirth": "1990-01-15",
    "passportNumber": "P12345678",
    "passportCountry": "CO",
})
conn = http.client.HTTPSConnection("api.verifik.co")
conn.request("GET", f"/v2/passport/us/entries?{params}", "", {})
res = conn.getresponse()
print(res.read().decode("utf-8"))
```

</TabItem>
</Tabs>

### レスポンス

<Tabs>
<TabItem value="200" label="200">

```json
{
  "data": {},
  "signature": {
    "dateTime": "October 10, 2025 7:10 PM",
    "message": "Certified by Verifik.co"
  },
  "id": "EXAMPLE"
}
```

</TabItem>
<TabItem value="404" label="404">

```json
{
  "code": "NotFound",
  "message": "Record not found."
}
```

</TabItem>
</Tabs>

### メモ

- 本プロダクトの**提供可否**は制限される場合があります。本番利用前にアカウント担当へ確認してください。
- **フィールド形式**（特に `dateOfBirth`）は、Verifik 統合の契約に従ってください。
