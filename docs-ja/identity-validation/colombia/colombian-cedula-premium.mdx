---
id: colombian-cedula-premium
title: 🇨🇴 コロンビア — 国民身分証プレミアム(CC)
description: "発行日不要のCC拡張照会。GET/POST /v2/co/cedula/premium。"
slug: /identity-validation/colombia/colombian-cedula-premium
---

export const structuredData = {
	"@context": "https://schema.org",
	"@type": "TechArticle",
	headline: "コロンビア国民身分証プレミアム(CC) | Verifik KYC API",
	description:
		"発行日を自動的に解決し、cédula extraと同じ拡張フィールドを返します。",
	articleSection: "API ドキュメント",
	keywords: "コロンビア, CC, cédula, プレミアム, Verifik",
	about: {
		"@type": "Thing",
		name: "本人確認 API",
	},
};

<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(structuredData) }} />

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

### エンドポイント

```
GET https://api.verifik.co/v2/co/cedula/premium
```

`GET https://verifik.app/v2/co/cedula/premium`。**POST**も同じパラメータで可。

**CC専用**。**`documentNumber`**のみ（5～10桁）。

### ヘッダー

| 名前 | 値 |
| --- | --- |
| Accept | `application/json` |
| Authorization | `Bearer <token>` |

### リクエスト例

<Tabs>
  <TabItem value="get" label="GET">

```javascript
import axios from "axios";

const { data } = await axios.get("https://api.verifik.co/v2/co/cedula/premium", {
	params: { documentNumber: "1234567890" },
	headers: {
		Accept: "application/json",
		Authorization: `Bearer ${process.env.VERIFIK_TOKEN}`,
	},
});
console.log(data);
```

  </TabItem>
</Tabs>

英語の詳細: [Colombian cédula premium](https://docs.verifik.co/identity-validation/colombia/colombian-cedula-premium)。
