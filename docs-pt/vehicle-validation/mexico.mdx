---
id: vehicle-validation-mexico-alt
title: México
description: Verificar informações de veículo no México pela placa
slug: /vehicle-validation/mexico
---

# México

### Endpoint

```
https://api.verifik.co/v2/mx/vehiculo/placa
```

O serviço de informações veiculares retorna dados detalhados sobre um veículo no México com base na placa: marca, modelo, ano, VIN e outros detalhes relacionados. Passe a placa no parâmetro de consulta `plate` em uma requisição GET; a resposta inclui campos orientados ao registro quando há correspondência.

### Cabeçalhos

| Nome          | Valor              |
| ------------- | ------------------ |
| Content-Type  | `application/json` |
| Authorization | `Bearer <token>`   |

### Parâmetros

| Nome  | Tipo   | Obrigatório | Descrição                    | Exemplo   |
| ----- | ------ | ----------- | ---------------------------- | --------- |
| plate | String | Sim         | Placa do veículo a consultar. | `ABC1234` |

### Requisição

```javascript
import axios from 'axios';

const options = {
  method: 'GET',
  url: 'https://api.verifik.co/v2/mx/vehiculo/placa',
  params: {plate: 'ABC1234'},
  headers: {
    Accept: 'application/json',
    Authorization: 'Bearer <your_token>'
  }
};

try {
  const { data } = await axios.request(options);
  console.log(data);
} catch (error) {
  console.error(error);
}
```

### Resposta

```json
{
  "success": true,
  "data": {
    "plate": "ABC1234",
    "make": "Nissan",
    "model": "Sentra",
    "year": "2021",
    "engine": "2.0L",
    "transmission": "CVT",
    "fuelType": "Gasoline",
    "vin": "1N4AL3AP8JC123456",
    "registrationDate": "10/08/2021",
    "status": "active",
    "owner": "Carlos Rodríguez",
    "state": "Jalisco",
    "city": "Guadalajara"
  }
}
```

### Erros

```json
{
  "success": false,
  "error": "Vehicle not found",
  "code": "VEHICLE_NOT_FOUND"
}
```

## Casos de uso

- **Seguros**: verificar detalhes do veículo para apólices
- **Concessionárias**: validar informações antes da compra
- **Frotas**: acompanhar veículos corporativos
- **Autoridades**: verificar registro e propriedade
- **Due diligence**: validar dados em transações
