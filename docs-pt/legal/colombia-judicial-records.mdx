---
id: colombia-judicial-records
title: 🇨🇴 Colômbia — Antecedentes judiciais (expedientes)
description: Consulte expedientes judiciais na Colômbia por CC, cidade e número (Rama Judicial).
sidebar_label: 🇨🇴 Antecedentes judiciais
slug: /legal/colombia-judicial-records
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

### Endpoint

```
GET https://api.verifik.co/v2/co/rama/juzgado/expedientes
```

Envie **`documentType`** (somente **`CC`**), **`documentNumber`** e **`city`** na **query** (ex.: `BOGOTA`).

### Cabeçalhos

| Name | Value |
| --- | --- |
| Accept | `application/json` |
| Authorization | `Bearer <token>` |

### Parâmetros

| name | type | required | description |
| --- | --- | --- | --- |
| `documentType` | string | yes | Apenas **`CC`**. |
| `documentNumber` | string | yes | Número a consultar. |
| `city` | string | yes | Código da cidade/circuito (ex.: `BOGOTA`, `MEDELLIN`). |

### Requisição

<Tabs>
  <TabItem value="node" label="Node.js">

```javascript
import axios from "axios";

const { data } = await axios.get("https://api.verifik.co/v2/co/rama/juzgado/expedientes", {
	params: { documentType: "CC", documentNumber: "1234567890", city: "BOGOTA" },
	headers: {
		Accept: "application/json",
		Authorization: `Bearer ${process.env.VERIFIK_TOKEN}`,
	},
});
console.log(data);
```

  </TabItem>
  <TabItem value="python" label="Python">

```python
import os, requests

url = "https://api.verifik.co/v2/co/rama/juzgado/expedientes"
headers = {"Accept": "application/json", "Authorization": f"Bearer {os.getenv('VERIFIK_TOKEN')}"}
params = {"documentType": "CC", "documentNumber": "1234567890", "city": "BOGOTA"}
r = requests.get(url, headers=headers, params=params)
print(r.json())
```

  </TabItem>
</Tabs>

### Resposta

<Tabs>
  <TabItem value="200" label="200">

```json
{
	"data": {
		"documentType": "CC",
		"documentNumber": "1234567890",
		"city": "BOGOTA",
		"courtOfepms": "EPMS EXAMPLE",
		"filingNumber": "2024-000123"
	},
	"signature": {
		"message": "Certified by Verifik.co",
		"dateTime": "March 3, 2022 3:55 PM"
	},
	"id": "ABC12"
}
```

  </TabItem>
  <TabItem value="404" label="404">

```json
{
	"code": "NotFound",
	"message": "Record not found."
}
```

  </TabItem>
</Tabs>

### Notas

- A API aceita apenas **`documentType`** **`CC`**.
- **`city`** deve ser um código suportado pela integração.
