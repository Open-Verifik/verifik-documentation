---
id: colombia-situacion-militar
title: 🇨🇴 Colômbia — Situação militar
description: Consulte a situação militar na Colômbia com cédula de cidadania (CC).
sidebar_label: 🇨🇴 Situação militar
slug: /identity/colombia-situacion-militar
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

### Endpoint

```
GET https://api.verifik.co/v2/co/situacion-militar
```

Envie **`documentType=CC`** e **`documentNumber`** como **query**. Apenas **CC** (Cédula de Ciudadanía) é aceito.

### Cabeçalhos

| Name | Value |
| --- | --- |
| Accept | `application/json` |
| Authorization | `Bearer <token>` |

### Parâmetros

| name | type | required | description |
| --- | --- | --- | --- |
| `documentType` | string | yes | Deve ser `CC`. CE, PPT etc. são rejeitados neste endpoint. |
| `documentNumber` | string | yes | Número da cédula a consultar. |

### Requisição

<Tabs>
  <TabItem value="node" label="Node.js">

```javascript
import axios from "axios";

const { data } = await axios.get("https://api.verifik.co/v2/co/situacion-militar", {
	params: { documentType: "CC", documentNumber: "1121329661" },
	headers: {
		Accept: "application/json",
		Authorization: `Bearer ${process.env.VERIFIK_TOKEN}`,
	},
});
console.log(data);
```

  </TabItem>
  <TabItem value="python" label="Python">

```python
import os, requests

url = "https://api.verifik.co/v2/co/situacion-militar"
headers = {"Accept": "application/json", "Authorization": f"Bearer {os.getenv('VERIFIK_TOKEN')}"}
params = {"documentType": "CC", "documentNumber": "1121329661"}
r = requests.get(url, headers=headers, params=params)
print(r.json())
```

  </TabItem>
</Tabs>

### Resposta

<Tabs>
  <TabItem value="200" label="200">

```json
{
	"data": {
		"adress": "Endereço ou texto orientativo conforme a fonte",
		"documentNumber": "1121329661",
		"documentType": "CC",
		"fullName": "SOBRENOME NOME",
		"place": "Texto de local do registro",
		"remissSince": "Data ou texto de status quando aplicável",
		"state": "Texto da situação militar"
	},
	"signature": {
		"message": "Certified by Verifik.co",
		"dateTime": "April 20, 2026 10:00 AM"
	},
	"id": "ABC12"
}
```

  </TabItem>
  <TabItem value="404" label="404">

```json
{
	"code": "NotFound",
	"message": "Record not found."
}
```

  </TabItem>
  <TabItem value="409" label="409">

```json
{
	"code": "MissingParameter",
	"message": "Erro de validação (parâmetro ausente ou documentType diferente de CC)."
}
```

  </TabItem>
</Tabs>

O campo `adress` segue a grafia retornada pela API.

### Notas

- Somente **`CC`** é válido para **`documentType`**.
- O conteúdo dos campos depende do registro na fonte oficial.
