# Sitemap and Domain Configuration

## ✅ Fixed Issues

### 1. Domain Update
- **Changed**: `documentation.verifik.co` → `docs.verifik.co`
- **Updated in**:
  - `docusaurus.config.js` - Main URL configuration
  - `static/robots.txt` - Sitemap reference and comments

### 2. Sitemap Index File
- **Created**: Custom plugin to generate `sitemap_index.xml`
- **Location**: `src/plugins/sitemap-index.js`
- **Purpose**: Generates `sitemap_index.xml` that references the main `sitemap.xml` (as required by SEO standards)

## 📍 File Locations

After build, the following files will be available:

- ✅ `https://docs.verifik.co/robots.txt` - Search engine directives
- ✅ `https://docs.verifik.co/sitemap.xml` - Main sitemap (auto-generated by Docusaurus)
- ✅ `https://docs.verifik.co/sitemap_index.xml` - Sitemap index (generated by our plugin)

## 🔍 How It Works

1. **Docusaurus** automatically generates `sitemap.xml` during build
2. **Our custom plugin** generates `sitemap_index.xml` that references the main sitemap
3. **robots.txt** points to `sitemap_index.xml` as required

## 🧪 Testing

After building the site:

```bash
npm run build
```

Verify the files exist:
- `build/sitemap.xml` (auto-generated)
- `build/sitemap_index.xml` (generated by plugin)
- `build/robots.txt` (from static folder)

Test the URLs:
- `https://docs.verifik.co/robots.txt`
- `https://docs.verifik.co/sitemap.xml`
- `https://docs.verifik.co/sitemap_index.xml`

## 📋 For Your SEO Guy

**Confirm the following URLs are accessible:**
1. ✅ `https://docs.verifik.co/robots.txt`
2. ✅ `https://docs.verifik.co/sitemap_index.xml`

**Note**: 
- `sitemap_index.xml` references the main `sitemap.xml`
- Both files are generated automatically during build
- The domain has been updated from `documentation.verifik.co` to `docs.verifik.co` everywhere

## 🔄 If You Need to Rebuild

After making these changes, you'll need to:
1. Rebuild the site: `npm run build`
2. Deploy the new build
3. Verify the URLs are accessible

The plugin will automatically generate `sitemap_index.xml` on every build.

