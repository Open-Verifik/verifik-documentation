---
id: colombian-cedula-premium
title: 🇨🇴 哥伦比亚 — 国民身份证高级核验(CC)
description: "无需手动提供签发日期的 CC 扩展查询。GET/POST /v2/co/cedula/premium。"
slug: /identity-validation/colombia/colombian-cedula-premium
---

export const structuredData = {
	"@context": "https://schema.org",
	"@type": "TechArticle",
	headline: "哥伦比亚国民身份证高级核验(CC) | Verifik KYC API",
	description:
		"自动解析签发日期后，返回与 cédula extra 一致的扩展字段。",
	articleSection: "API 文档",
	keywords: "哥伦比亚, CC, cédula, 高级, Verifik",
	about: {
		"@type": "Thing",
		name: "身份核验 API",
	},
};

<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(structuredData) }} />

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

### 端点

```
GET https://api.verifik.co/v2/co/cedula/premium
```

`GET https://verifik.app/v2/co/cedula/premium`。支持相同参数的 **POST**。

**仅 CC**：只需 **`documentNumber`**（5～10 位数字）。

### 请求头

| 名称 | 值 |
| --- | --- |
| Accept | `application/json` |
| Authorization | `Bearer <token>` |

### 请求示例

<Tabs>
  <TabItem value="get" label="GET">

```javascript
import axios from "axios";

const { data } = await axios.get("https://api.verifik.co/v2/co/cedula/premium", {
	params: { documentNumber: "1234567890" },
	headers: {
		Accept: "application/json",
		Authorization: `Bearer ${process.env.VERIFIK_TOKEN}`,
	},
});
console.log(data);
```

  </TabItem>
</Tabs>

完整响应与错误说明见英文文档 [Colombian cédula premium](https://docs.verifik.co/identity-validation/colombia/colombian-cedula-premium)。
