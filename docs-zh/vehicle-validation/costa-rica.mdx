---
id: costa-rica
title: 哥斯达黎加 — 车辆（号牌）
description: 通过 GET /v2/cr/vehicle 按号牌查询车辆。
slug: /vehicle-validation/costa-rica
---

export const structuredData = {
	"@context": "https://schema.org",
	"@type": "TechArticle",
	headline: "哥斯达黎加车辆查询 | Verifik API",
	description: "按 plate 查询：GET /v2/cr/vehicle。",
	articleSection: "API 文档",
	keywords: "哥斯达黎加 车辆, v2/cr/vehicle",
	about: {
		"@type": "Thing",
		name: "车辆验证 API",
	},
};

<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(structuredData) }} />

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# 哥斯达黎加车辆查询

### 端点

```
https://api.verifik.co/v2/cr/vehicle
```

必填 **`plate`**。

### 请求头

| 名称 | 值 |
| --- | --- |
| Accept | `application/json` |
| Authorization | `Bearer <token>` |

### 参数

| 名称 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `plate` | string | 是 | 号牌（无空格） |

### 请求

<Tabs>
  <TabItem value="node" label="Node.js">

```javascript
import axios from "axios";

const { data } = await axios.get("https://api.verifik.co/v2/cr/vehicle", {
  params: { plate: "ABC123" },
  headers: { Accept: "application/json", Authorization: `Bearer ${process.env.VERIFIK_TOKEN}` },
});
console.log(data);
```

  </TabItem>
</Tabs>

### 响应

<Tabs>
  <TabItem value="200" label="200">

```json
{ "data": {}, "signature": {}, "id": "" }
```

  </TabItem>
</Tabs>
