---
id: vehicle-validation-colombia-runt-vehicle-by-vin
title: RUNT — 按 VIN 查询车辆
description: 通过哥伦比亚 RUNT 使用车辆识别代号（VIN）查询完整历史与技术信息。
sidebar_label: RUNT — 车辆(VIN)
slug: /vehicle-validation/colombia/runt-vehicle-by-vin
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

### 端点

```
GET https://api.verifik.co/v2/co/runt/vehicle-by-vin
```

将 **`vin`** 作为 **查询参数** 传入，从哥伦比亚 **RUNT** 获取车辆的 **完整历史与技术报告**。适用于暂无车牌或需用 VIN 核对实车的场景。

### 请求头

| 名称 | 值 |
| --- | --- |
| Accept | `application/json` |
| Authorization | `Bearer <token>` |

### 参数

| 名称 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `vin` | string | 是 | 需查询的 **17 位 VIN**（无空格，与 RUNT 登记一致）。 |

### 请求

<Tabs>
  <TabItem value="node" label="Node.js">

```javascript
import axios from "axios";

const { data } = await axios.get("https://api.verifik.co/v2/co/runt/vehicle-by-vin", {
	params: { vin: "JTEBH9FJ7DK091425" },
	headers: {
		Accept: "application/json",
		Authorization: `Bearer ${process.env.VERIFIK_TOKEN}`,
	},
});
console.log(data);
```

  </TabItem>
  <TabItem value="python" label="Python">

```python
import os, requests

url = "https://api.verifik.co/v2/co/runt/vehicle-by-vin"
headers = {"Accept": "application/json", "Authorization": f"Bearer {os.getenv('VERIFIK_TOKEN')}"}
params = {"vin": "JTEBH9FJ7DK091425"}
r = requests.get(url, headers=headers, params=params)
print(r.json())
```

  </TabItem>
</Tabs>

### 响应

<Tabs>
  <TabItem value="200" label="200">

```json
{
	"data": {
		"informacionGeneral": {
			"estadoDelVehiculo": "ACTIVO",
			"marca": "TOYOTA",
			"linea": "PRADO",
			"modelo": "2013",
			"noVin": "JTEBH9FJ7DK091425",
			"noPlaca": "NET603",
			"organismoTransito": "SECRETARIA DISTRITAL DE MOVILIDAD DE BOGOTA"
		},
		"vin": "JTEBH9FJ7DK091425"
	},
	"signature": {
		"message": "Certified by Verifik.co",
		"dateTime": "October 10, 2025 8:12 PM"
	},
	"id": "BJ24Z"
}
```

  </TabItem>
  <TabItem value="404" label="404">

```json
{
	"code": "NotFound",
	"message": "Record not found."
}
```

  </TabItem>
  <TabItem value="409" label="409">

```json
{
	"code": "MissingParameter",
	"message": "missing vin"
}
```

  </TabItem>
</Tabs>

### 说明

- **VIN** 为车辆的 **17 位**识别代号。
- 响应可包含一般信息、SOAT、年检等 **RUNT** 返回的字段（视数据源而定）。
- **`ACTIVO`** 表示登记有效（示例）。
