---
id: chile
title: 智利 — 车辆（号牌）
description: 通过 GET /v2/cl/vehicle 按号牌查询。
slug: /vehicle-validation/chile
---

export const structuredData = {
	"@context": "https://schema.org",
	"@type": "TechArticle",
	headline: "智利车辆查询 | Verifik API",
	description: "按 plate 查询：GET /v2/cl/vehicle。",
	articleSection: "API 文档",
	keywords: "智利 车辆, v2/cl/vehicle",
	about: {
		"@type": "Thing",
		name: "车辆验证 API",
	},
};

<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(structuredData) }} />

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# 智利车辆信息

### Endpoint

```
https://api.verifik.co/v2/cl/vehicle
```

必填 **`plate`**。

### Headers

| Name | Value |
| --- | --- |
| Accept | `application/json` |
| Authorization | `Bearer <token>` |

### Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `plate` | string | Yes | 号牌（无空格或点） |

### Request

<Tabs>
  <TabItem value="node" label="Node.js">

```javascript
import axios from "axios";

const { data } = await axios.get("https://api.verifik.co/v2/cl/vehicle", {
  params: { plate: "BB985" },
  headers: { Accept: "application/json", Authorization: `Bearer ${process.env.VERIFIK_TOKEN}` },
});
console.log(data);
```

  </TabItem>
</Tabs>

### Response

<Tabs>
  <TabItem value="200" label="200">

```json
{ "data": {}, "signature": {} }
```

  </TabItem>
</Tabs>
