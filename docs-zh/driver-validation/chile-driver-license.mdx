---
id: chile-driver-license
title: 智利 — 驾驶证
description: 通过 GET /v2/cl/driver-license 按驾驶证号查询。
slug: /driver-validation/chile-driver-license
---

export const structuredData = {
	"@context": "https://schema.org",
	"@type": "TechArticle",
	headline: "智利驾驶证查询 | Verifik API",
	description: "GET /v2/cl/driver-license，documentNumber。",
	articleSection: "API 文档",
	keywords: "智利 驾驶证, v2/cl/driver-license",
	about: {
		"@type": "Thing",
		name: "驾驶证 API",
	},
};

<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(structuredData) }} />

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# 智利驾驶证

### Endpoint

```
https://api.verifik.co/v2/cl/driver-license
```

使用 **`documentNumber`** 查询。

### Headers

| Name | Value |
| --- | --- |
| Accept | `application/json` |
| Authorization | `Bearer <token>` |

### Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `documentNumber` | string | Yes | 驾驶证号码 |

### Request

<Tabs>
  <TabItem value="node" label="Node.js">

```javascript
import axios from "axios";

const { data } = await axios.get("https://api.verifik.co/v2/cl/driver-license", {
  params: { documentNumber: "12345678" },
  headers: { Accept: "application/json", Authorization: `Bearer ${process.env.VERIFIK_TOKEN}` },
});
console.log(data);
```

  </TabItem>
</Tabs>

### Response

<Tabs>
  <TabItem value="200" label="200">

```json
{ "data": {}, "signature": {}, "id": "" }
```

  </TabItem>
</Tabs>
