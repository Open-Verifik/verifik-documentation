---
id: identity-validation-colombia-colombian-politically-exposed-persons
title: 🇨🇴 Colombie — Consultation PEP (AML)
description: Vérifiez le statut PPE (personne politiquement exposée) pour CC ou NIT.
sidebar_label: 🇨🇴 PEP (AML)
slug: /identity-validation/colombia/colombian-politically-exposed-persons
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

### Point d'accès

```
GET https://api.verifik.co/v2/co/politically-exposed-persons
```

Dépistage **AML** des **PPE** (personnes politiquement exposées). Ce n’est **pas** le permis de séjour **PEP** — voir [Colombie — PEP (séjour)](/verifik-fr/identity/colombia-pep-id).

### En-têtes

| Name | Value |
| --- | --- |
| Accept | `application/json` |
| Authorization | `Bearer <token>` |

### Paramètres

| name | type | required | description |
| --- | --- | --- | --- |
| `documentType` | string | yes | **`CC`** (personne) ou **`NIT`** (entreprise). |
| `documentNumber` | string | yes | Numéro sans espaces ni ponctuation. |

### Requête

<Tabs>
  <TabItem value="node" label="Node.js">

```javascript
import axios from "axios";

const { data } = await axios.get("https://api.verifik.co/v2/co/politically-exposed-persons", {
	params: { documentType: "CC", documentNumber: "123456789" },
	headers: {
		Accept: "application/json",
		Authorization: `Bearer ${process.env.VERIFIK_TOKEN}`,
	},
});
console.log(data);
```

  </TabItem>
  <TabItem value="python" label="Python">

```python
import os, requests

url = "https://api.verifik.co/v2/co/politically-exposed-persons"
headers = {"Accept": "application/json", "Authorization": f"Bearer {os.getenv('VERIFIK_TOKEN')}"}
params = {"documentType": "CC", "documentNumber": "123456789"}
r = requests.get(url, headers=headers, params=params)
print(r.json())
```

  </TabItem>
</Tabs>

### Réponse

<Tabs>
  <TabItem value="200" label="200">

```json
{
	"data": {
		"documentType": "CC",
		"documentNumber": "123456789",
		"detail": []
	},
	"signature": {
		"message": "Certified by Verifik.co",
		"dateTime": "August 22, 2023 8:02 PM"
	},
	"id": "PEP001"
}
```

  </TabItem>
  <TabItem value="404" label="404">

```json
{
	"code": "NotFound",
	"message": "Record not found."
}
```

  </TabItem>
  <TabItem value="409" label="409">

```json
{
	"code": "MissingParameter",
	"message": "documentType must be one of: [CC, NIT]"
}
```

  </TabItem>
</Tabs>

### Remarques

- **PEP (AML)** ≠ permis de séjour **PEP** (voir lien ci-dessus).
- **`documentType`** : **`CC`** ou **`NIT`** uniquement.
