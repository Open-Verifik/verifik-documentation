---
id: costa-rica
title: 코스타리카 기업(NITE)
description: GET /v2/cr/company로 NITE 법인 조회(KYB).
slug: /business-validation/costa-rica
---

export const structuredData = {
	"@context": "https://schema.org",
	"@type": "TechArticle",
	headline: "코스타리카 기업 조회(NITE) | Verifik KYB API",
	description: "NITE로 법인 조회: GET /v2/cr/company.",
	articleSection: "API 문서",
	keywords: "코스타리카 NITE, KYB, v2/cr/company",
	about: {
		"@type": "Thing",
		name: "기업 검증 API",
	},
};

<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(structuredData) }} />

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# 코스타리카 기업 조회(NITE)

### 엔드포인트

```
https://api.verifik.co/v2/cr/company
```

**`documentType`**: `NITE`, **`documentNumber`**: NITE 번호.

### 헤더

| 이름 | 값 |
| --- | --- |
| Accept | `application/json` |
| Authorization | `Bearer <token>` |

### 매개변수

| 이름 | 유형 | 필수 | 설명 |
| --- | --- | --- | --- |
| `documentType` | string | 예 | `NITE` |
| `documentNumber` | string | 예 | NITE 식별자 |

### 요청

<Tabs>
  <TabItem value="node" label="Node.js">

```javascript
import axios from "axios";

const { data } = await axios.get("https://api.verifik.co/v2/cr/company", {
  params: { documentType: "NITE", documentNumber: "3101122876" },
  headers: { Accept: "application/json", Authorization: `Bearer ${process.env.VERIFIK_TOKEN}` },
});
console.log(data);
```

  </TabItem>
</Tabs>

### 응답

<Tabs>
  <TabItem value="200" label="200">

```json
{ "data": {}, "signature": {}, "id": "" }
```

  </TabItem>
</Tabs>
