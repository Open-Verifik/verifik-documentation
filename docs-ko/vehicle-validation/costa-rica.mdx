---
id: costa-rica
title: 코스타리카 — 차량(번호판)
description: GET /v2/cr/vehicle로 번호판 조회.
slug: /vehicle-validation/costa-rica
---

export const structuredData = {
	"@context": "https://schema.org",
	"@type": "TechArticle",
	headline: "코스타리카 차량 조회 | Verifik API",
	description: "번호판 plate로 조회: GET /v2/cr/vehicle.",
	articleSection: "API 문서",
	keywords: "코스타리카 차량, v2/cr/vehicle",
	about: {
		"@type": "Thing",
		name: "차량 검증 API",
	},
};

<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(structuredData) }} />

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# 코스타리카 차량 조회

### 엔드포인트

```
https://api.verifik.co/v2/cr/vehicle
```

필수 **`plate`**.

### 헤더

| 이름 | 값 |
| --- | --- |
| Accept | `application/json` |
| Authorization | `Bearer <token>` |

### 매개변수

| 이름 | 유형 | 필수 | 설명 |
| --- | --- | --- | --- |
| `plate` | string | 예 | 번호판(공백 없음) |

### 요청

<Tabs>
  <TabItem value="node" label="Node.js">

```javascript
import axios from "axios";

const { data } = await axios.get("https://api.verifik.co/v2/cr/vehicle", {
  params: { plate: "ABC123" },
  headers: { Accept: "application/json", Authorization: `Bearer ${process.env.VERIFIK_TOKEN}` },
});
console.log(data);
```

  </TabItem>
</Tabs>

### 응답

<Tabs>
  <TabItem value="200" label="200">

```json
{ "data": {}, "signature": {}, "id": "" }
```

  </TabItem>
</Tabs>
