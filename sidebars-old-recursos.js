/**
 * Hidden sidebar for old /recursos/ paths
 * These files are accessible via direct URLs but not shown in navigation
 */
module.exports = {
	// Empty sidebar - all pages are hidden from navigation
};
