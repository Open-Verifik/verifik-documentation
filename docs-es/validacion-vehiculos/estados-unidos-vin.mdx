---
id: estados-unidos-vin
title: 🇺🇸 Estados Unidos - Vehículo por VIN
description: Validación de vehículos en EE. UU. por VIN de 17 caracteres (decodificación NHTSA)
slug: /validacion-vehiculos/estados-unidos-vin
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# 🇺🇸 Estados Unidos - Vehículo por VIN

Este servicio decodifica un **VIN de 17 caracteres** para vehículos del mercado estadounidense y devuelve atributos estructurados (marca, modelo, año, motor, clase de carrocería y campos relacionados) a partir de datos tipo NHTSA VPIC.

### Endpoint

```
GET https://api.verifik.co/v2/usa/vehicle-by-vin
```

Decodifica por **VIN** con el parámetro `vin` (exactamente 17 caracteres). El parámetro opcional `force` puede forzar una decodificación nueva en lugar de usar un registro almacenado, cuando la integración lo permita.

### Headers

| Name          | Value              |
| ------------- | ------------------ |
| Content-Type  | `application/json` |
| Authorization | `Bearer <token>`   |

### Parámetros

| Name | Type | Required | Description | Example |
| ---- | ---- | -------- | ----------- | ------- |
| `vin` | string | **Sí** | VIN de 17 caracteres (letras y números, sin espacios). | `1G3AK47A7CM535209` |
| `force` | string | No | Si es `true`, fuerza una decodificación nueva en lugar de devolver una coincidencia almacenada (si aplica). | `true` |

### Solicitud

<Tabs>
  <TabItem value="javascript" label="JavaScript">

```javascript
import axios from 'axios';

const options = {
  method: 'GET',
  url: 'https://api.verifik.co/v2/usa/vehicle-by-vin',
  params: {
    vin: '1G3AK47A7CM535209'
  },
  headers: {
    'Accept': 'application/json',
    'Authorization': 'Bearer <tu_token>'
  }
};

try {
  const { data } = await axios.request(options);
  console.log(data);
} catch (error) {
  console.error(error);
}
```

  </TabItem>
  <TabItem value="python" label="Python">

```python
import http.client

conn = http.client.HTTPSConnection("api.verifik.co")
payload = ''
headers = {}
conn.request("GET", "/v2/usa/vehicle-by-vin?vin=1G3AK47A7CM535209", payload, headers)
res = conn.getresponse()
data = res.read()
print(data.decode("utf-8"))
```

  </TabItem>
</Tabs>

### Respuesta

<Tabs>
  <TabItem value="200" label="200">

```json
{
  "data": {
    "make": "OLDSMOBILE",
    "model": "Cutlass",
    "modelYear": "1982",
    "vehicleType": "PASSENGER CAR",
    "vin": "1G3AK47A7CM535209"
  },
  "signature": {
    "dateTime": "October 10, 2025 7:10 PM",
    "message": "Certified by Verifik.co"
  },
  "id": "KFOFL"
}
```

  </TabItem>
  <TabItem value="404" label="404">

```json
{
    "code": "NotFound",
    "message": "Record not found."
}
```

  </TabItem>
</Tabs>
